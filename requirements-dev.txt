-r requirements.txt
ruff==0.15.20
# Import-direction contracts in pyproject.toml ([tool.importlinter]); run: lint-imports
import-linter==2.15
# Progress bar for the one-off migrate_dataset_to_object_storage command;
# imported lazily there and not needed by the running application.
tqdm==4.66.4
# Lets `manage.py test --parallel` report a failing test instead of crashing on it.
tblib==3.2.2
