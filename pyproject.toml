# Lint configuration only — this repo is not a pip-installable package.
[tool.ruff]
target-version = "py311"
extend-exclude = [
    "staticfiles",
    "logs",
    "scripts",
    "*/migrations/*",
]

[tool.ruff.lint]
# Permissive baseline on purpose: syntax errors and undefined names only.
# The codebase mixes tabs/spaces and predates any formatter — no style
# enforcement here. Widening this ruleset is a separate, deliberate PR.
select = ["E9", "F63", "F7", "F82"]
